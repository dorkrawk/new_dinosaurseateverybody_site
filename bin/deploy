#!/usr/bin/env bash

# run jekyl commands
cd jekyll_blog
jekyll build

# deploy via capitrano
# bundle exec cap production deploy

# deploy via Heroku
# git push heroku master

cd ..
# deply via fly.io
flyctl deploy
